---
import StatusTerminal from "./StatusTerminal.astro";
import KeyboardBase from "./KeyboardBase.astro";
import EmojiDock from "./EmojiDock.astro";

const now = new Date();
const stamp = now.toLocaleDateString("en-US", {
  year: "numeric",
  month: "short",
  day: "numeric",
  timeZone: "UTC",
});

const FONT_URL = "https://jordannewell.github.io/newell-typeface/";

// 12 keys spelling JORDAN NEWELL — split left (JORDAN) / right (NEWELL) gutters.
// Premium keycap look: matte finish, black inner cap, alternating frame colors.
const LEFT = ["J", "O", "R", "D", "A", "N"];
const RIGHT = ["N", "E", "W", "E", "L", "L"];
const keys = [
  // left gutter — JORDAN, pulled inboard to frame the terminal
  { top: "13vh", left: "20vw", rot: -7,  variant: "green" }, // J
  { top: "21vh", left: "24vw", rot:  3,  variant: "white" }, // O
  { top: "28vh", left: "18vw", rot: -3,  variant: "green" }, // R
  { top: "72vh", left: "19vw", rot:  4,  variant: "white" }, // D
  { top: "80vh", left: "24vw", rot: -5,  variant: "green" }, // A
  { top: "88vh", left: "18vw", rot:  2,  variant: "white" }, // N
  // right gutter — NEWELL, mirrored
  { top: "14vh", right: "21vw", rot:  5,  variant: "white" }, // N
  { top: "22vh", right: "25vw", rot: -4,  variant: "green" }, // E
  { top: "29vh", right: "19vw", rot:  6,  variant: "white" }, // W
  { top: "73vh", right: "20vw", rot: -6,  variant: "green" }, // E
  { top: "81vh", right: "25vw", rot:  3,  variant: "white" }, // L
  { top: "89vh", right: "19vw", rot: -2,  variant: "green" }, // L
];
const LETTERS = [...LEFT, ...RIGHT];
---

<section class="cs">
  {keys.map((k, i) => (
    <a
      class={`cs-key cs-key-${k.variant}`}
      href={FONT_URL}
      target="_blank"
      rel="noopener"
      aria-label="Newell typeface — GitHub repo"
      title="Newell typeface — GitHub repo"
      style={`--k-top:${k.top ?? "auto"};--k-right:${k.right ?? "auto"};--k-left:${k.left ?? "auto"};--k-rot:${k.rot}deg;`}
    >
      <span class="cs-key-cap">{LETTERS[i]}</span>
    </a>
  ))}

  <div class="cs-logo-wrap" aria-hidden="true" style="display:none">
    <img
      src="/images/coming-soon/n-mark.png"
      alt=""
      class="cs-logo"
      width="579"
      height="668"
      loading="eager"
      decoding="async"
    />
  </div>

  <StatusTerminal />

  {/* Mobile-only single-row key strip — J O R D A N N E W E L L.
      Sits under the terminal. Hidden on desktop (scattered fixed keys take over). */}
  <div class="cs-key-strip" aria-label="Newell typeface keys">
    <div class="cs-strip-word">
      {LEFT.map((letter, i) => (
        <a
          class={`cs-strip-key cs-key-${i % 2 === 0 ? "green" : "white"}`}
          href={FONT_URL}
          target="_blank"
          rel="noopener"
          aria-label="Newell typeface"
          title="Newell typeface"
        >
          <span class="cs-strip-cap">{letter}</span>
        </a>
      ))}
      {RIGHT.map((letter, i) => (
        <a
          class={`cs-strip-key cs-key-${i % 2 === 0 ? "white" : "green"}`}
          href={FONT_URL}
          target="_blank"
          rel="noopener"
          aria-label="Newell typeface"
          title="Newell typeface"
        >
          <span class="cs-strip-cap">{letter}</span>
        </a>
      ))}
    </div>
  </div>

  {/* Mechanical keyboard in 3D perspective — desk gradient removed per request.
      Stream Deck is a child of keyboard-stage so it's absolutely positioned
      relative to it, scrolling with the page instead of floating on viewport. */}
  <div class="cs-keyboard-stage">
    <div class="cs-keyboard">
      <KeyboardBase />
    </div>
    <div class="cs-streamdeck" aria-label="NEWELL product family">
      <div class="cs-sd-bezel">
        <div class="cs-sd-grid cs-sd-grid-6">
          <a class="cs-sd-key cs-sd-c1" href="https://jordannewell.github.io/claudeway-site/" target="_blank" rel="noopener" title="claudeway">
            <span class="cs-sd-cap">CL</span>
            <span class="cs-sd-label">claudeway</span>
          </a>
          <a class="cs-sd-key cs-sd-c3" href="https://jordannewell.github.io/curtis-ai-chat/" target="_blank" rel="noopener" title="curtis-ai-chat">
            <span class="cs-sd-cap">CA</span>
            <span class="cs-sd-label">curtis-ai</span>
          </a>
          <a class="cs-sd-key cs-sd-c5" href="https://jordannewell.github.io/crypto-key-classifier/" target="_blank" rel="noopener" title="crypto-key-classifier">
            <span class="cs-sd-cap">K</span>
            <span class="cs-sd-label">crypto-key</span>
          </a>
          <a class="cs-sd-key cs-sd-c7" href="https://jordannewell.github.io/newell-typeface/" target="_blank" rel="noopener" title="newell-typeface">
            <span class="cs-sd-cap">N</span>
            <span class="cs-sd-label">typeface</span>
          </a>
          <a class="cs-sd-key cs-sd-c10" href="https://jordannewell.github.io/git-hygiene/" target="_blank" rel="noopener" title="git-hygiene">
            <span class="cs-sd-cap">G</span>
            <span class="cs-sd-label">git-hygiene</span>
          </a>
          <a class="cs-sd-key cs-sd-c2" href="https://jordannewell.github.io/pat-scanner/" target="_blank" rel="noopener" title="pat-scanner">
            <span class="cs-sd-cap">P</span>
            <span class="cs-sd-label">pat-scan</span>
          </a>
        </div>
        {/* FX rack — inside the same bezel as the launch keys.
            Top 2 slots: LED indicators (pulse on any effect trigger).
            Bottom 2 slots: unique effects not on the keyboard. */}
        <div class="cs-fx-rack" aria-label="Effects and indicators">
          <div class="cs-fx-led" aria-hidden="true" data-led="1"></div>
          <button class="cs-fx-key cs-fx-music" data-music-toggle aria-label="Open music player" title="Music player">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M9 18V5l12-2v13"/>
              <circle cx="6" cy="18" r="3"/>
              <circle cx="18" cy="16" r="3"/>
            </svg>
          </button>
          <button class="cs-fx-key" data-fx="spotlight" aria-label="Spotlight a random launch key" title="Spotlight">
            <img class="cs-fx-key-img" src="/images/coming-soon/nmoji-button.png" alt="" width="14" height="14" decoding="async" loading="lazy">
          </button>
          <button class="cs-fx-key" data-fx="glitch" aria-label="Glitch the launch pad" title="Glitch">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/>
            </svg>
          </button>
        </div>
      </div>
      {/* Launch label under the keypad bezel */}
      <a class="cs-sd-launch" href="#" aria-hidden="true">Launch <span aria-hidden="true">→</span></a>
    </div>
  </div>

  {/* Curtis AI badge bottom-left — mirrors the PFP. Same hover treatment
      (lift + neon green glow), links to the Obsidian plugin page. */}
  <a
    href="https://community.obsidian.md/plugins/curtis-ai-chat"
    target="_blank"
    rel="noopener"
    class="cs-curtis"
    aria-label="Curtis AI"
    title="Curtis AI"
    style="display:none"
  >
    <img src="/images/curtis-ai-corner.png?v=4" alt="Curtis AI" width="155" height="120" loading="lazy" decoding="async" />
  </a>

  {/* Mobile-only "Newell font →" lives inside keyboard-stage now (between
      keyboard and stream deck). Desktop typelink also inside keyboard-stage. */}


</section>

{/* Vintage power knob — bottom-of-page ceremonial power button.
    Desktop only (mobile uses the header button). Outside .cs so it survives
    the CRT-collapse animation. Styled as a chunky rotary knob with pointer
    indicator — visually distinct from the header's discreet shadcn circle. */}
<footer class="cs-bottom-bar">
  <div class="cs-controls-row">
    <button class="cs-audio-btn" data-audio-toggle aria-pressed="false" aria-label="Toggle ambient audio" title="Ambient audio (opt-in)">
      <svg class="cs-audio-icon cs-audio-off" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"/>
        <line x1="22" y1="9" x2="16" y2="15"/>
        <line x1="16" y1="9" x2="22" y2="15"/>
      </svg>
      <svg class="cs-audio-icon cs-audio-on" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"/>
        <path d="M15.54 8.46a5 5 0 0 1 0 7.07"/>
        <path d="M19.07 4.93a10 10 0 0 1 0 14.14"/>
      </svg>
    </button>
    <div class="cs-knob-col">
      <button class="cs-power-btn cs-power-btn-vintage" aria-label="Power toggle" title="Power">
        <span class="cs-power-btn-vintage-icon" aria-hidden="true"></span>
      </button>
      <div class="cs-knob-name" aria-hidden="true">
        <span>JORDAN</span>
        <span>NEWELL</span>
      </div>
    </div>
  </div>
</footer>

<script src="/js/status-hero.js?v=5" defer></script>
<script src="/js/kb-shortcuts.js?v=34" defer></script>

<style is:global>
  @font-face {
    font-family: "Newell";
    src: url("/fonts/Newell-Regular.woff2") format("woff2");
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }
</style>

<style is:global>
  body { background-color: #000; }
  .dot-grid { display: none; }
  main { padding-top: 10px !important; padding-bottom: 0 !important; max-width: none !important; }
  /* Lock scroll on desktop only — mobile needs to scroll to reach the dock. */
  @media (min-width: 769px) {
    html, body { min-height: 100%; }
  }
  /* Dead-on-arrival: page ships with cs-powered-off, but CSS transitions
     on the off-state rules would animate from default → off on first
     paint, creating a flash of the alive state. Disable transitions
     until first frame is done (cs-ready added by JS). Animations
     (shimmer, pulse) still play — only transitions are gated. */
  body:not(.cs-ready) * {
    transition: none !important;
  }
</style>

<style>
  .cs {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 10px;
    padding: 0 0 32px;
    /* Fit fully inside viewport on desktop. Header (~60px) + main padding (~28px)
       leave ~calc(100vh - 90px) for splash content. Body overflow:hidden backs this up. */
    min-height: calc(100vh - 90px);
  }
  .cs :global([data-terminal]) {
    margin-top: 0;
  }
  .cs-keyboard-stage {
    width: 100%;
    max-width: 500px; /* match keyboard visual (452px SVG + scale + buffer) so stream deck anchors next to keys */
    margin-top: 0;
    position: relative;
    perspective: 1200px;
    perspective-origin: 50% 20%;
  }
  .cs-keyboard {
    width: 100%;
    display: flex;
    justify-content: center;
    position: relative;
    z-index: 2;
    transform: translateX(-10px) scale(1.12);
    transform-origin: center 80%;
    margin-top: 10px;
    transition: box-shadow 0.3s ease,
                filter 0.5s ease,
                opacity 0.5s ease;
    filter: drop-shadow(0 18px 18px rgba(0, 0, 0, 0.75));
  }
  /* SVG has explicit width/height attrs (452px) — cap to parent so it
     shrinks on narrow mobile viewports instead of overflowing right. */
  .cs-keyboard :global(svg.kb) {
    max-width: 100%;
    height: auto;
  }
  /* Stationary 3D pose — hover just magnifies, no tilt change.
     Use direct .cs-keyboard:hover (not stage:hover) so hovering the stream deck
     inside the stage doesn't enlarge the keyboard. */
  .cs-keyboard:hover {
    transform: translateX(-10px) scale(1.14);
    filter: drop-shadow(0 28px 26px rgba(0, 0, 0, 0.8))
            drop-shadow(0 0 14px rgba(63, 255, 70, 0.06));
  }
  /* Mobile: keyboard under terminal, keypad hidden, letters under keyboard.
     Scoped block (not is:global) so this wins over the base .cs-keyboard-stage rule above. */
  @media (max-width: 768px) {
    .cs-keyboard-stage {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 40px;
      order: 2;
      width: 100%;
      max-width: 360px;
      margin: 24px 10px 0;
      padding: 0 10px;
      transform: none;
      overflow: visible;
    }
    /* Keyboard at natural size — matches keypad bezel dimensions. */
    .cs-keyboard {
      display: flex;
      width: 100%;
      margin: 0 !important;
      transform: none !important;
      filter: drop-shadow(0 8px 14px rgba(0, 0, 0, 0.65));
    }
    .cs-keyboard:hover { transform: none !important; }
    .cs-keyboard svg.kb {
      width: 100%;
      max-width: 100%;
      height: auto;
    }
    /* Keypad keys at 2:1 (more rectangular than 16:9) so bezel is
       VISIBLY shorter than the keyboard — no more perception that
       keypad is taller. */
    .cs-sd-grid .cs-sd-key { aspect-ratio: 2 / 1; }
    .cs-streamdeck {
      display: block;
      position: static;
      width: 100%;
      max-width: none;
      margin: 0;
    }
    /* Grid grows to fill the bezel — without this, flex parent leaves
       empty space on the right of the grid + FX rack. */
    .cs-sd-bezel .cs-sd-grid { flex: 1 1 auto; }
  }
  /* Desktop: 25px gap from terminal — scoped so it beats the base margin-top:0. */
  @media (min-width: 769px) {
    .cs-keyboard-stage {
      margin-top: 25px;
    }
  }
  /* Desk surface — a tilted plane that recedes from foreground to background.
     Drawn as a CSS gradient: light hit near the keyboard, fading to deep shadow
     toward the back. Slight green tint near the keyboard's underglow. */
  .cs-desk {
    position: absolute;
    left: 50%;
    bottom: -10px;
    width: 110%;
    height: 80px;
    transform: translateX(-50%) perspective(600px) rotateX(60deg);
    transform-origin: center top;
    background:
      radial-gradient(ellipse at 50% 0%,
        rgba(63, 255, 70, 0.10) 0%,
        rgba(20, 20, 22, 0.6) 25%,
        rgba(8, 8, 10, 0.95) 60%,
        rgba(0, 0, 0, 1) 100%);
    z-index: 1;
    pointer-events: none;
  }
  /* Subtle desk grain via repeating gradient overlay */
  .cs-desk::before {
    content: "";
    position: absolute;
    inset: 0;
    background: repeating-linear-gradient(
      90deg,
      transparent 0px,
      transparent 2px,
      rgba(255, 255, 255, 0.012) 2px,
      rgba(255, 255, 255, 0.012) 3px
    );
  }/* Curtis AI badge — visual sibling of the PFP, anchored bottom-left. */
  .cs-curtis {
    position: fixed;
    left: 28px;
    bottom: 0;
    width: 205px;
    height: auto;
    text-decoration: none;
    transition: transform 0.2s ease, filter 0.2s ease;
    z-index: 50;
    filter: drop-shadow(0 8px 16px rgba(0, 0, 0, 0.55));
  }
  .cs-curtis img {
    width: 100%;
    height: auto;
    display: block;
  }
  .cs-curtis:hover {
    transform: translateY(-4px);
    filter: drop-shadow(0 14px 22px rgba(0, 0, 0, 0.7))
            drop-shadow(0 0 18px rgba(63, 255, 70, 0.25));
  }
  @media (max-width: 768px) {
    .cs-curtis { width: 110px; left: 12px; bottom: 0; }
  }
  .cs-logo-wrap {
    width: 100%;
    max-width: 220px;
    display: block;
    pointer-events: none;
    user-select: none;
  }
  .cs-logo {
    width: 100%;
    height: auto;
    display: block;
  }
  @media (max-width: 480px) {
    .cs-logo-wrap { max-width: 220px; }
  }
  .cs-meta {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
  }
  .cs-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--color-green);
    background: color-mix(in srgb, var(--color-green) 12%, transparent);
    border: 1px solid color-mix(in srgb, var(--color-green) 35%, transparent);
    padding: 4px 10px;
  }
  .cs-eyebrow-lg {
    font-size: 0.9rem;
    letter-spacing: 0.22em;
    padding: 6px 14px;
    gap: 10px;
    box-shadow: 0 0 24px color-mix(in srgb, var(--color-green) 18%, transparent);
  }
  .cs-eyebrow-lg .cs-pulse {
    width: 8px;
    height: 8px;
  }
  .cs-pulse {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-green);
    box-shadow: 0 0 8px var(--color-green);
    animation: cs-blink 2s ease-in-out infinite;
  }
  @keyframes cs-blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.3; }
  }
  .cs-stamp {
    font-family: var(--font-mono);
    font-size: 0.65rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-fg-muted);
    margin: 0;
  }
  /* Tiny status line anchoring the top of the page — no full header. */
  .cs-status-line {
    font-family: var(--font-mono);
    font-size: 0.6rem;
    letter-spacing: 0.28em;
    text-transform: uppercase;
    color: var(--color-fg-muted);
    opacity: 0.5;
    text-align: center;
    margin-bottom: 4px;
  }

  /* Premium mechanical key-caps — matte PBT, black inner cap, colored frame + legend.
     Two variants alternate: green-frame (white legend) and white-frame (green legend).
     Inspired by GMK/SP profiles, not cheap ABS toys — sharp edges, subtle bevel,
     minimal depth, no glossy highlights. */
  .cs-key {
    position: fixed;
    top: var(--k-top, auto);
    right: var(--k-right, auto);
    left: var(--k-left, auto);
    transform: rotate(var(--k-rot, 0deg));
    transform-origin: center;
    width: 46px;
    height: 46px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    border-radius: 5px;
    transition: transform 0.14s cubic-bezier(0.2, 0.9, 0.4, 1.2),
                opacity 0.18s ease;
    opacity: 0.45;
    z-index: 5;
    user-select: none;
  }
  .cs-key:hover {
    opacity: 1;
  }
  /* Inner cap = black for both variants (PBT-style matte top) */
  .cs-key-cap {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    border-radius: 3px;
    background: #0a0a0a;
    font-family: "Newell", var(--font-mono);
    font-size: 1.25rem;
    line-height: 1;
    letter-spacing: -0.02em;
    transition: background 0.14s, color 0.14s;
  }
  /* Variant A: green frame, white legend */
  .cs-key-green {
    background: #0a0a0a; /* thin black ring */
    box-shadow:
      0 0 0 1.5px #3FFF46,           /* outer rim = neon green frame */
      0 1px 0 #3FFF46,                /* subtle lift */
      0 3px 5px rgba(0, 0, 0, 0.6),
      0 0 12px rgba(63, 255, 70, 0.15);
  }
  .cs-key-green .cs-key-cap {
    color: #fff;
    box-shadow: inset 0 0 0 1px #1a1a1a; /* hairline inner edge */
  }
  /* Variant B: white frame, green legend */
  .cs-key-white {
    background: #0a0a0a;
    box-shadow:
      0 0 0 1.5px #fafafa,             /* outer rim = off-white frame */
      0 1px 0 #fafafa,
      0 3px 5px rgba(0, 0, 0, 0.6),
      0 0 12px rgba(255, 255, 255, 0.08);
  }
  .cs-key-white .cs-key-cap {
    color: #3FFF46;
    box-shadow: inset 0 0 0 1px #1a1a1a;
  }
  /* Hover: tiny lift, frame brightens */
  .cs-key:hover {
    transform: rotate(var(--k-rot, 0deg)) translateY(-2px) scale(1.06);
  }
  .cs-key-green:hover {
    box-shadow:
      0 0 0 1.5px #5fff66,
      0 3px 0 #3FFF46,
      0 6px 10px rgba(0, 0, 0, 0.7),
      0 0 22px rgba(63, 255, 70, 0.4);
  }
  .cs-key-white:hover {
    box-shadow:
      0 0 0 1.5px #ffffff,
      0 3px 0 #fafafa,
      0 6px 10px rgba(0, 0, 0, 0.7),
      0 0 22px rgba(255, 255, 255, 0.18);
  }
  /* Press: sink, no lift */
  .cs-key:active {
    transform: rotate(var(--k-rot, 0deg)) translateY(1px) scale(0.97);
  }
  .cs-key-green:active {
    box-shadow:
      0 0 0 1.5px #3FFF46,
      0 1px 0 #29a62e,
      0 2px 3px rgba(0, 0, 0, 0.5),
      0 0 8px rgba(63, 255, 70, 0.1);
  }
  .cs-key-white:active {
    box-shadow:
      0 0 0 1.5px #fafafa,
      0 1px 0 #c0c0c0,
      0 2px 3px rgba(0, 0, 0, 0.5);
  }
  /* Hide on phones — scattered fixed keys clutter a 375px viewport.
     The horizontal .cs-key-strip below replaces them on mobile. */
  @media (max-width: 768px) {
    .cs-key { display: none; }
  }
  /* Tighter keys on tablets so they don't crowd centered content */
  @media (min-width: 769px) and (max-width: 1100px) {
    .cs-key { width: 38px; height: 38px; }
    .cs-key-cap { width: 30px; height: 30px; font-size: 1rem; }
  }

  /* Mobile-only key strip — single row of 12 (J O R D A N N E W E L L) */
  .cs-key-strip {
    display: none; /* hidden on desktop, scattered fixed keys take over */
    flex-direction: row;
    justify-content: center;
    align-items: center;
    gap: 3px;
    padding: 4px 2px 8px;
  }
  .cs-strip-word {
    display: inline-flex;
    align-items: center;
    gap: 3px;
  }
  .cs-strip-key {
    flex: 0 0 auto;
    width: 24px;
    height: 24px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    border-radius: 4px;
    background: #0a0a0a;
    transition: transform 0.18s cubic-bezier(0.34, 1.56, 0.64, 1),
                box-shadow 0.18s ease;
  }
  .cs-strip-key:hover,
  .cs-strip-key:active {
    transform: scale(1.18);
    z-index: 2;
  }
  .cs-strip-key.cs-key-green:hover,
  .cs-strip-key.cs-key-green:active {
    box-shadow: 0 0 0 1.2px #3FFF46, 0 1px 2px rgba(0,0,0,0.5), 0 0 14px rgba(63,255,70,0.4);
  }
  .cs-strip-key.cs-key-white:hover,
  .cs-strip-key.cs-key-white:active {
    box-shadow: 0 0 0 1.2px #fafafa, 0 1px 2px rgba(0,0,0,0.5), 0 0 14px rgba(255,255,255,0.25);
  }
  .cs-strip-cap {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 18px;
    height: 18px;
    border-radius: 2px;
    background: #0a0a0a;
    font-family: "Newell", var(--font-mono);
    font-size: 0.7rem;
    line-height: 1;
  }
  /* Same variant styling as fixed keys but with thinner rims for small sizes */
  .cs-strip-key.cs-key-green {
    box-shadow: 0 0 0 1.2px #3FFF46, 0 1px 2px rgba(0,0,0,0.5), 0 0 8px rgba(63,255,70,0.15);
  }
  .cs-strip-key.cs-key-green .cs-strip-cap { color: #fff; }
  .cs-strip-key.cs-key-white {
    box-shadow: 0 0 0 1.2px #fafafa, 0 1px 2px rgba(0,0,0,0.5);
  }
  .cs-strip-key.cs-key-white .cs-strip-cap { color: #3FFF46; }
  /* :active subsumed by the :hover/:active block above — both enlarge now. */
  @media (max-width: 768px) {
    .cs-key-strip {
      display: none; /* redundant now that keyboard visual is hidden and NEWELL wordmark at bottom spells it out */
    }
  }

  /* "Newell font →" — rendered in the Newell typeface itself.
     Mobile copy lives inside .cs-key-strip (under the letters).
     Desktop copy is a separate element under the keyboard. */
  .cs-typelink {
    font-family: "Newell", var(--font-mono);
    font-size: 0.95rem;
    letter-spacing: -0.01em;
    color: var(--color-fg-muted);
    text-decoration: none;
    transition: color 0.15s, border-color 0.15s;
    margin-top: 10px;
  }
  .cs-typelink-desktop {
    display: inline-flex;
    margin-top: 10px;
  }
  /* Mobile-only typelink: hidden on desktop, shown under terminal on mobile */
  .cs-typelink-mobile {
    display: none;
  }
  @media (max-width: 768px) {
    .cs-typelink-mobile {
      display: inline-flex;
      margin-top: 6px;
      font-family: "JetBrains Mono", monospace;
      font-size: 0.55rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: #606060;
    }
    .cs-typelink-desktop {
      display: none;
    }
  }
  /* Desktop: typelink is full-width block centered under keyboard (overrides
     the scoped inline-flex base above by source order at equal specificity). */
  @media (min-width: 769px) {
    .cs-typelink-desktop {
      display: block;
      width: 100%;
      text-align: center;
      margin: 12px 0 0;
      font-family: "JetBrains Mono", monospace;
      font-size: 0.55rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: #606060;
    }
  }
  .cs-typelink:hover {
    color: #3FFF46;
    border-bottom-color: #3FFF46;
  }
  .cs-typelink span[aria-hidden] {
    display: inline-block;
    margin-left: 4px;
    transition: transform 0.15s;
  }
  .cs-typelink:hover span[aria-hidden] {
    transform: translateX(2px);
  }
</style>

<style is:global>
  .cs-streamdeck {
    width: 180px;
    margin: 0 auto;
    perspective: 1200px;
    perspective-origin: 50% 20%;
    transition: filter 0.5s ease, opacity 0.5s ease;
  }
  /* GREEN bezel outline (matches keyboard green theme) — flat, no rotateX.
     Flex container so the deck grid + FX rack sit side-by-side as ONE unit. */
  .cs-sd-bezel {
    display: flex;
    gap: 6px;
    align-items: stretch;
    background: #060606;
    border-radius: 8px;
    padding: 10px;
    box-shadow:
      inset 0 1px 0 rgba(0, 255, 65, 0.06),
      0 0 0 1.5px #3FFF46,
      0 3px 0 #0D0D0D,
      0 8px 16px rgba(0, 0, 0, 0.7),
      0 0 20px rgba(0, 255, 65, 0.06);
    transform: none;
    transition: box-shadow 0.3s ease;
  }
  .cs-streamdeck:hover .cs-sd-bezel {
    box-shadow:
      inset 0 1px 0 rgba(0, 255, 65, 0.1),
      0 0 0 1.5px #3FFF46,
      0 4px 0 #0D0D0D,
      0 12px 24px rgba(0, 0, 0, 0.8),
      0 0 18px rgba(0, 255, 65, 0.12),
      0 0 18px rgba(255, 255, 255, 0.12);
  }
  .cs-sd-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 5px; }
  .cs-sd-key {
    aspect-ratio: 1;
    display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 2px;
    text-decoration: none; border-radius: 4px; background: #0a0a0a;
    transition: transform 0.14s cubic-bezier(0.2, 0.9, 0.4, 1.2), box-shadow 0.14s ease;
    position: relative;
    /* Firefox renders text poorly inside 3D-transformed parents — force own layer. */
    transform: translateZ(0);
    will-change: transform;
  }
  .cs-sd-cap, .cs-sd-label { backface-visibility: hidden; }
  .cs-sd-key:hover { transform: translateY(-3px) scale(1.06); z-index: 5; }
  .cs-sd-key:active { transform: translateY(0) scale(0.97); }
  /* MULTICOLOR neon keycap outlines */
  .cs-sd-c1 { box-shadow: 0 0 0 1.5px #00FF41, 0 1px 0 #00FF41, 0 3px 5px rgba(0,0,0,0.6), 0 0 10px rgba(0,255,65,0.15); }
  .cs-sd-c2 { box-shadow: 0 0 0 1.5px #3B82F6, 0 1px 0 #3B82F6, 0 3px 5px rgba(0,0,0,0.6), 0 0 10px rgba(59,130,246,0.15); }
  .cs-sd-c3 { box-shadow: 0 0 0 1.5px #06B6D4, 0 1px 0 #06B6D4, 0 3px 5px rgba(0,0,0,0.6), 0 0 10px rgba(6,182,212,0.15); }
  .cs-sd-c4 { box-shadow: 0 0 0 1.5px #A855F7, 0 1px 0 #A855F7, 0 3px 5px rgba(0,0,0,0.6), 0 0 10px rgba(168,85,247,0.15); }
  .cs-sd-c5 { box-shadow: 0 0 0 1.5px #00FF41, 0 1px 0 #00FF41, 0 3px 5px rgba(0,0,0,0.6), 0 0 10px rgba(0,255,65,0.15); }
  .cs-sd-c6 { box-shadow: 0 0 0 1.5px #3B82F6, 0 1px 0 #3B82F6, 0 3px 5px rgba(0,0,0,0.6), 0 0 10px rgba(59,130,246,0.15); }
  .cs-sd-c7 { box-shadow: 0 0 0 1.5px #06B6D4, 0 1px 0 #06B6D4, 0 3px 5px rgba(0,0,0,0.6), 0 0 10px rgba(6,182,212,0.15); }
  .cs-sd-c8 { box-shadow: 0 0 0 1.5px #00FF41, 0 1px 0 #00FF41, 0 3px 5px rgba(0,0,0,0.6), 0 0 10px rgba(0,255,65,0.15); }
  .cs-sd-c9 { box-shadow: 0 0 0 1.5px #F59E0B, 0 1px 0 #F59E0B, 0 3px 5px rgba(0,0,0,0.6), 0 0 10px rgba(245,158,11,0.15); }
  .cs-sd-c10 { box-shadow: 0 0 0 1.5px #fafafa, 0 1px 0 #fafafa, 0 3px 5px rgba(0,0,0,0.6), 0 0 10px rgba(255,255,255,0.10); }
  /* Hover: brighten each color */
  .cs-sd-c1:hover { box-shadow: 0 0 0 2px #00FF41, 0 4px 0 rgba(0,255,65,0.4), 0 6px 10px rgba(0,0,0,0.7), 0 0 20px rgba(0,255,65,0.4); }
  .cs-sd-c2:hover { box-shadow: 0 0 0 2px #3B82F6, 0 4px 0 rgba(59,130,246,0.4), 0 6px 10px rgba(0,0,0,0.7), 0 0 20px rgba(59,130,246,0.4); }
  .cs-sd-c3:hover { box-shadow: 0 0 0 2px #06B6D4, 0 4px 0 rgba(6,182,212,0.4), 0 6px 10px rgba(0,0,0,0.7), 0 0 20px rgba(6,182,212,0.4); }
  .cs-sd-c4:hover { box-shadow: 0 0 0 2px #A855F7, 0 4px 0 rgba(168,85,247,0.4), 0 6px 10px rgba(0,0,0,0.7), 0 0 20px rgba(168,85,247,0.4); }
  .cs-sd-c5:hover { box-shadow: 0 0 0 2px #00FF41, 0 4px 0 rgba(0,255,65,0.4), 0 6px 10px rgba(0,0,0,0.7), 0 0 20px rgba(0,255,65,0.4); }
  .cs-sd-c6:hover { box-shadow: 0 0 0 2px #3B82F6, 0 4px 0 rgba(59,130,246,0.4), 0 6px 10px rgba(0,0,0,0.7), 0 0 20px rgba(59,130,246,0.4); }
  .cs-sd-c7:hover { box-shadow: 0 0 0 2px #06B6D4, 0 4px 0 rgba(6,182,212,0.4), 0 6px 10px rgba(0,0,0,0.7), 0 0 20px rgba(6,182,212,0.4); }
  .cs-sd-c8:hover { box-shadow: 0 0 0 2px #00FF41, 0 4px 0 rgba(0,255,65,0.4), 0 6px 10px rgba(0,0,0,0.7), 0 0 20px rgba(0,255,65,0.4); }
  .cs-sd-c9:hover { box-shadow: 0 0 0 2px #F59E0B, 0 4px 0 rgba(245,158,11,0.4), 0 6px 10px rgba(0,0,0,0.7), 0 0 20px rgba(245,158,11,0.4); }
  .cs-sd-c10:hover { box-shadow: 0 0 0 2px #fafafa, 0 4px 0 rgba(250,250,250,0.4), 0 6px 10px rgba(0,0,0,0.7), 0 0 20px rgba(255,255,255,0.25); }
  /* Bright readable caps */
  .cs-sd-cap { font-family: "JetBrains Mono", monospace; font-size: 0.95rem; font-weight: 700; line-height: 1; color: #FFFFFF; }
  .cs-sd-label { font-family: "JetBrains Mono", monospace; font-size: 0.55rem; color: #B0B0B0; letter-spacing: 0.02em; }
  .cs-sd-c1:hover .cs-sd-cap, .cs-sd-c5:hover .cs-sd-cap, .cs-sd-c8:hover .cs-sd-cap { color: #00FF41; }
  .cs-sd-c2:hover .cs-sd-cap, .cs-sd-c6:hover .cs-sd-cap { color: #3B82F6; }
  .cs-sd-c3:hover .cs-sd-cap, .cs-sd-c7:hover .cs-sd-cap { color: #06B6D4; }
  .cs-sd-c4:hover .cs-sd-cap { color: #A855F7; }
  .cs-sd-c9:hover .cs-sd-cap { color: #F59E0B; }
  .cs-sd-c10:hover .cs-sd-cap { color: #fafafa; }
  .cs-sd-key:hover .cs-sd-label { color: #FFFFFF; }

  /* Desktop: 2-col × 3-row stream deck (6 launch keys) + 4 FX buttons share
     one bezel as a single launch-pad unit. 168px wide total. Sits beside the
     keyboard, fits within terminal width. */
  @media (min-width: 769px) {
    .cs-streamdeck {
      position: absolute;
      left: 100%;
      top: -2.5px;
      margin-left: 13px;
      z-index: 6;
      width: 168px;
    }
    .cs-sd-grid.cs-sd-grid-6 {
      grid-template-columns: repeat(2, 1fr);
    }
    .cs-keyboard-stage {
      transform: translateX(-89px);
      margin-top: 25px;
      text-align: center;
    }
  }
  /* Launch pad label under stream deck bezel */
  .cs-sd-launch {
    display: block;
    text-align: center;
    font-family: "JetBrains Mono", monospace;
    font-size: 0.55rem;
    color: #606060;
    margin-top: 6px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    text-decoration: none;
  }

  /* FX rack — flex child of the bezel (sits inside the same green frame
     as the launch keys). Top 2 slots are LED indicators, bottom 2 are
     effect triggers (spotlight + glitch) unique to the FX rack. */
  .cs-fx-rack {
    display: flex;
    flex-direction: column;
    gap: 5px;
    width: 34px;
    /* Subtle vertical divider between deck and FX */
    border-left: 1px solid rgba(63, 255, 70, 0.18);
    padding-left: 6px;
  }
  /* LED indicator — pulses green when any effect fires */
  .cs-fx-led {
    flex: 1 1 0;
    width: 100%;
    border-radius: 4px;
    background: #0a0a0a;
    border: 1px solid rgba(63, 255, 70, 0.2);
    position: relative;
    transition: border-color 0.3s ease, background 0.3s ease;
  }
  .cs-fx-led::after {
    content: "";
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: rgba(63, 255, 70, 0.18);
    transition: all 0.22s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .cs-fx-led.is-lit {
    border-color: #3FFF46;
    background: rgba(63, 255, 70, 0.08);
  }
  .cs-fx-led.is-lit::after {
    background: #3FFF46;
    width: 14px;
    height: 14px;
    box-shadow:
      0 0 8px #3FFF46,
      0 0 16px rgba(63, 255, 70, 0.7),
      inset 0 0 4px rgba(255, 255, 255, 0.4);
  }
  /* Spotlight: random launch key pops + glows green briefly */
  .cs-sd-key.cs-sd-spotlight {
    animation: cs-sd-spotlight 1.5s cubic-bezier(0.2, 0.8, 0.4, 1);
    z-index: 10;
  }
  @keyframes cs-sd-spotlight {
    0%, 100% {
      transform: translateY(0) scale(1);
      filter: brightness(1);
    }
    30% {
      transform: translateY(-8px) scale(1.3);
      filter: brightness(1.5) drop-shadow(0 0 18px #3FFF46) drop-shadow(0 0 32px rgba(63, 255, 70, 0.6));
    }
  }
  /* Glitch: bezel distorts briefly (skew + hue shift + jitter).
     Bezel base transform is `none` so we can freely animate it. */
  .cs-sd-bezel.cs-glitch {
    animation: cs-glitch 0.5s steps(5);
  }
  @keyframes cs-glitch {
    0%   { transform: translate(0, 0)    skewX(0deg);  filter: none; }
    18%  { transform: translate(-3px, 1px) skewX(2deg);  filter: hue-rotate(90deg) saturate(2); }
    36%  { transform: translate(2px, -2px) skewX(-2deg); filter: invert(0.12) contrast(1.3); }
    54%  { transform: translate(-2px, 2px) skewX(1deg);  filter: hue-rotate(-90deg) brightness(1.3); }
    72%  { transform: translate(3px, -1px) skewX(-1deg); filter: saturate(2.5); }
    100% { transform: translate(0, 0)    skewX(0deg);  filter: none; }
  }
  .cs-fx-key {
    flex: 1 1 0;
    width: 100%;
    border-radius: 4px;
    background: #0a0a0a;
    border: 1px solid rgba(63, 255, 70, 0.35);
    color: #3FFF46;
    cursor: pointer;
    padding: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.05),
      0 1px 2px rgba(0, 0, 0, 0.6);
    transition: transform 0.14s cubic-bezier(0.2, 0.9, 0.4, 1.2),
                box-shadow 0.14s ease,
                border-color 0.14s ease,
                color 0.14s ease;
  }
  .cs-fx-key svg {
    width: 14px;
    height: 14px;
    display: block;
  }
  .cs-fx-key-img {
    width: 18px;
    height: 18px;
    display: block;
    object-fit: contain;
    filter: drop-shadow(0 0 4px rgba(63, 255, 70, 0.5));
  }
  .cs-fx-key:hover {
    transform: translateY(-2px);
    border-color: #3FFF46;
    color: #fff;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.1),
      0 0 0 1px #3FFF46,
      0 3px 6px rgba(0, 0, 0, 0.7),
      0 0 12px rgba(63, 255, 70, 0.4);
  }
  .cs-fx-key:active {
    transform: translateY(0);
    color: #3FFF46;
    box-shadow:
      inset 0 1px 2px rgba(0, 0, 0, 0.6),
      0 0 0 1px rgba(63, 255, 70, 0.5);
  }
  .cs-fx-key:focus-visible {
    outline: 2px solid #3FFF46;
    outline-offset: 2px;
  }
  /* Bottom bar — vintage power knob + audio toggle. In-flow at the bottom
     of the page. Desktop only. Outside .cs so CRT-collapse doesn't capture it. */
  .cs-bottom-bar {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 28px;
    padding: 32px 16px 40px;
  }
  .cs-controls-row {
    display: flex;
    align-items: center;
    gap: 28px;
  }
  .cs-knob-col {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
  }
  /* Audio toggle — small push-button next to the knob. Off by default. */
  .cs-audio-btn {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    background:
      radial-gradient(circle at 35% 28%, #1f1f1f 0%, #0a0a0a 55%, #000 100%);
    border: 1.5px solid #1f1f1f;
    color: #707070;
    cursor: pointer;
    padding: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: transform 0.2s cubic-bezier(0.4, 0, 0.2, 1),
                box-shadow 0.2s cubic-bezier(0.4, 0, 0.2, 1),
                border-color 0.2s ease,
                color 0.2s ease;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.08),
      inset 0 -1px 0 rgba(0, 0, 0, 0.6),
      0 2px 4px rgba(0, 0, 0, 0.6);
  }
  .cs-audio-btn:hover {
    transform: translateY(-1px);
    border-color: rgba(63, 255, 70, 0.4);
    color: #aaa;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.1),
      0 3px 6px rgba(0, 0, 0, 0.7);
  }
  .cs-audio-btn:focus-visible {
    outline: 2px solid #3FFF46;
    outline-offset: 3px;
  }
  .cs-audio-btn[aria-pressed="true"] {
    background:
      radial-gradient(circle at 35% 28%, rgba(63, 255, 70, 0.18) 0%, #0a0a0a 60%, #000 100%);
    border-color: rgba(63, 255, 70, 0.55);
    color: #3FFF46;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.08),
      inset 0 -1px 0 rgba(0, 0, 0, 0.6),
      0 2px 4px rgba(0, 0, 0, 0.6),
      0 0 14px rgba(63, 255, 70, 0.25);
  }
  .cs-audio-btn[aria-pressed="true"]:hover {
    border-color: #3FFF46;
    color: #5fff66;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.1),
      0 3px 6px rgba(0, 0, 0, 0.7),
      0 0 22px rgba(63, 255, 70, 0.5);
  }
  .cs-audio-icon {
    width: 18px;
    height: 18px;
    display: block;
  }
  /* Show the right icon based on pressed state */
  .cs-audio-btn[aria-pressed="false"] .cs-audio-on { display: none; }
  .cs-audio-btn[aria-pressed="true"] .cs-audio-off { display: none; }
  /* When power is OFF, audio button shows a SUBTLE dim red standby tint.
     No pulse animation — the knob is the beacon, audio button just
     matches the color story quietly. */
  body.cs-powered-off .cs-audio-btn {
    border-color: rgba(255, 64, 64, 0.28);
    color: rgba(255, 64, 64, 0.55);
  }
  /* Mobile: allow 2-page scroll. Terminal stays tall (the feature),
     JORDAN/NEWELL nameplate stays visible, comfortable padding. */
  @media (max-width: 768px) {
    /* Terminal body — keep tall (the feature), just slightly smaller
       than desktop's 500px for parity with mobile proportions. */
    .terminal .terminal-body {
      height: 400px !important;
      padding: 10px 14px 14px !important;
      font-size: 0.82rem !important;
      line-height: 1.55 !important;
    }
    .terminal .terminal-bar { padding: 6px 10px !important; }
    .terminal .terminal-foot { padding: 6px 12px 8px !important; font-size: 0.6rem !important; }
    /* Keyboard-stage: comfortable gap */
    .cs-keyboard-stage {
      gap: 28px !important;
      margin-top: 20px !important;
    }
    /* Bottom-bar: comfortable padding */
    .cs-bottom-bar { gap: 16px; padding: 24px 16px 32px; }
    .cs-controls-row { gap: 18px; }
    .cs-audio-btn { width: 44px; height: 44px; }
    .cs-audio-icon { width: 18px; height: 18px; }
  }

  /* Vintage power knob — chunky rotary tube-TV knob. Pseudo-elements render
     the rotary indicator (top pointer line) + center dot. Visually unmistakable
     vs header's discreet shadcn circle.
     Compound selector (.cs-power-btn.cs-power-btn-vintage) → (0,2,0) wins over
     base .cs-power-btn regardless of merge order. */
  .cs-power-btn.cs-power-btn-vintage {
    position: relative;
    width: 80px;
    height: 80px;
    border-radius: 50%;
    background:
      radial-gradient(circle at 35% 28%, #2c2c2c 0%, #0e0e0e 55%, #000 100%);
    border: 3px solid #1a1a1a;
    color: transparent;
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1),
                box-shadow 0.3s cubic-bezier(0.4, 0, 0.2, 1),
                border-color 0.3s ease;
    box-shadow:
      /* Outer bezel rings */
      0 0 0 4px #050505,
      0 0 0 6px #2f2f2f,
      /* Top inner highlight */
      inset 0 4px 8px rgba(255, 255, 255, 0.14),
      /* Bottom inner recess */
      inset 0 -5px 10px rgba(0, 0, 0, 0.9),
      /* Page drop shadow */
      0 6px 14px rgba(0, 0, 0, 0.75),
      0 16px 32px rgba(0, 0, 0, 0.55);
  }
  /* Rotary indicator line at top — like a volume knob pointer */
  .cs-power-btn.cs-power-btn-vintage .cs-power-btn-vintage-icon {
    position: absolute;
    top: 9px;
    left: 50%;
    transform: translateX(-50%);
    width: 4px;
    height: 14px;
    background: #3FFF46;
    border-radius: 2px;
    box-shadow: 0 0 6px #3FFF46, 0 0 14px rgba(63, 255, 70, 0.6);
    transition: opacity 0.3s ease;
  }
  /* Center dot via ::after on the button — knob spindle */
  .cs-power-btn.cs-power-btn-vintage::after {
    content: "";
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: radial-gradient(circle at 35% 35%, #5fff66 0%, #3FFF46 60%, #29a62e 100%);
    box-shadow:
      0 0 10px #3FFF46,
      0 0 20px rgba(63, 255, 70, 0.5),
      inset 0 -1px 2px rgba(0, 0, 0, 0.4);
  }
  .cs-power-btn.cs-power-btn-vintage:hover {
    transform: translateY(-2px);
    border-color: rgba(63, 255, 70, 0.5);
    box-shadow:
      0 0 0 4px #050505,
      0 0 0 6px #3a3a3a,
      inset 0 4px 8px rgba(255, 255, 255, 0.16),
      inset 0 -5px 10px rgba(0, 0, 0, 0.9),
      0 8px 18px rgba(0, 0, 0, 0.75),
      0 20px 38px rgba(0, 0, 0, 0.55),
      0 0 36px rgba(63, 255, 70, 0.4);
  }
  .cs-power-btn.cs-power-btn-vintage:active {
    transform: translateY(2px);
    box-shadow:
      0 0 0 4px #050505,
      0 0 0 6px #1a1a1a,
      inset 0 3px 6px rgba(0, 0, 0, 0.85),
      inset 0 -1px 2px rgba(0, 0, 0, 0.5),
      0 2px 4px rgba(0, 0, 0, 0.6);
  }
  .cs-power-btn.cs-power-btn-vintage:focus-visible {
    outline: 2px solid #3FFF46;
    outline-offset: 6px;
  }
  /* POWER label under the knob — vintage silkscreen text */
  .cs-power-btn-vintage-label {
    font-family: "JetBrains Mono", monospace;
    font-size: 0.6rem;
    letter-spacing: 0.4em;
    color: #606060;
    text-indent: 0.4em; /* visual balance for letter-spacing */
    text-transform: uppercase;
    user-select: none;
  }
  /* JORDAN / NEWELL stacked under POWER — Newell display face */
  .cs-knob-name {
    display: flex;
    flex-direction: column;
    align-items: center;
    line-height: 1;
    margin-top: 6px;
    font-family: "Newell", var(--font-mono);
    font-size: 0.85rem;
    letter-spacing: 0.04em;
    color: rgba(255, 255, 255, 0.35);
    transition: color 0.18s ease;
    user-select: none;
  }
  .cs-knob-name span { display: block; }
  .cs-knob-name span + span { margin-top: 1px; }
  .cs-bottom-bar:hover .cs-knob-name { color: rgba(63, 255, 70, 0.65); }
  /* When system is OFF, the nameplate becomes a shimmering beacon —
     moving green+white spotlight sweeps across both lines, drawing
     the eye to the power knob below. */
  body.cs-powered-off .cs-knob-name {
    background: linear-gradient(
      100deg,
      rgba(255, 255, 255, 0.18) 0%,
      rgba(63, 255, 70, 0.55) 35%,
      rgba(255, 255, 255, 0.95) 50%,
      rgba(63, 255, 70, 0.55) 65%,
      rgba(255, 255, 255, 0.18) 100%
    );
    background-size: 250% 100%;
    background-position: 200% 0;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    color: transparent;
    animation: cs-name-shimmer 3.5s linear infinite;
    filter: drop-shadow(0 0 6px rgba(63, 255, 70, 0.45));
  }
  @keyframes cs-name-shimmer {
    0%   { background-position: 200% 0; }
    100% { background-position: -50% 0; }
  }
  /* When screen is OFF, knob indicator goes red (standby) + label dims.
     Indicator pulses in sync with the header button's 1.8s red pulse. */
  body.cs-powered-off .cs-power-btn-vintage .cs-power-btn-vintage-icon {
    background: #ff4040;
    animation: cs-vintage-pulse 1.8s ease-in-out infinite;
  }
  @keyframes cs-vintage-pulse {
    0%, 100% { box-shadow: 0 0 6px #ff4040, 0 0 14px rgba(255, 64, 64, 0.6); }
    50%      { box-shadow: 0 0 10px #ff4040, 0 0 22px rgba(255, 64, 64, 0.95); }
  }
  body.cs-powered-off .cs-power-btn-vintage::after {
    background: radial-gradient(circle at 35% 35%, #ff7070 0%, #ff4040 60%, #a62929 100%);
    box-shadow: 0 0 10px #ff4040, 0 0 20px rgba(255, 64, 64, 0.5), inset 0 -1px 2px rgba(0, 0, 0, 0.4);
  }
  body.cs-powered-off .cs-power-btn-vintage-label {
    color: #ff4040;
    opacity: 0.7;
  }
  /* Mobile: scaled-down knob version (60px vs 80px desktop) */
  @media (max-width: 768px) {
    .cs-bottom-bar {
      padding: 24px 16px 32px;
    }
    .cs-power-btn.cs-power-btn-vintage {
      width: 60px;
      height: 60px;
      border-width: 2px;
      box-shadow:
        0 0 0 3px #050505,
        0 0 0 4px #2f2f2f,
        inset 0 3px 6px rgba(255, 255, 255, 0.14),
        inset 0 -4px 8px rgba(0, 0, 0, 0.9),
        0 4px 10px rgba(0, 0, 0, 0.75),
        0 10px 22px rgba(0, 0, 0, 0.55);
    }
    .cs-power-btn.cs-power-btn-vintage .cs-power-btn-vintage-icon {
      top: 7px;
      width: 3px;
      height: 10px;
    }
    .cs-power-btn.cs-power-btn-vintage::after {
      width: 8px;
      height: 8px;
    }
    .cs-power-btn-vintage-label {
      font-size: 0.5rem;
      letter-spacing: 0.3em;
    }
  }
  .cs-splash-wordmark {
    display: inline-flex;
    align-items: center;
    line-height: 0;
    opacity: 0.85;
    order: 99;
  }
  .cs-splash-wordmark img {
    display: block;
    height: 28px;
    width: auto;
    filter: drop-shadow(0 0 8px rgba(63, 255, 70, 0.18));
    transition: opacity 0.18s ease, filter 0.18s ease;
  }
  .cs-splash-wordmark:hover img {
    opacity: 1;
    filter: drop-shadow(0 0 12px rgba(63, 255, 70, 0.4));
  }
  @media (min-width: 640px) {
    .cs-splash-wordmark img { height: 34px; }
  }
  @media (max-width: 768px) {
    .cs-streamdeck { margin: 8px auto 0; max-width: 100%; }
    .cs-sd-bezel { transform: none; }
    /* Mobile: shrink keys slightly so bezel + FX rack fit narrower viewports */
    .cs-sd-cap { font-size: 1.1rem; }
    .cs-sd-label { font-size: 0.6rem; }
    .cs-fx-rack {
      width: 28px;
      border-left: 1px solid rgba(63, 255, 70, 0.18);
      padding-left: 5px;
    }
    .cs-fx-key svg, .cs-fx-key-img { width: 12px; height: 12px; }
    .cs-sd-launch {
      display: block;
      text-align: center;
      font-family: "JetBrains Mono", monospace;
      font-size: 0.55rem;
      color: #606060;
      margin-top: 6px;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      text-decoration: none;
    }
    /* Mobile: keyboard stacked ABOVE keypad, both visible. */
    .cs-keyboard-stage .cs-keyboard {
      display: flex;
      width: 100%;
      margin: 0 !important;
      transform: none !important;
    }
    .cs-keyboard-stage .cs-keyboard:hover { transform: none !important; }
    .cs-keyboard-stage .cs-streamdeck { display: block; position: static; margin: 0; }
    .cs-keyboard-stage .cs-keyboard svg.kb { width: 100%; max-width: 100%; height: auto; }
    .cs-key-strip { order: 3; }
    .cs-typelink-mobile { order: 4; }
    .cs :global([data-terminal]) { margin-top: 40px; }
  }
</style>

<style is:global>
  /* Self-typing JORDANNEWELL — flashes the empty socket green when "typed" */
  .kb-socket.kb-typed {
    fill: #3FFF46 !important;
    filter: drop-shadow(0 0 6px #3FFF46) drop-shadow(0 0 14px rgba(63,255,70,0.5));
    transition: fill 0.06s ease-out, filter 0.06s ease-out;
  }

  /* CRT scanline sweep */
  .kb-scanline {
    position: fixed;
    top: 0; left: 0; right: 0;
    height: 4px;
    background: linear-gradient(to bottom, transparent, #3FFF46 50%, transparent);
    box-shadow: 0 0 24px #3FFF46, 0 0 60px rgba(63,255,70,0.6);
    pointer-events: none;
    z-index: 99999;
    animation: kb-scanline-sweep 0.75s linear forwards;
  }
  @keyframes kb-scanline-sweep {
    from { transform: translateY(-4px); }
    to   { transform: translateY(100vh); }
  }

  /* Scattered letter spin */
  .cs-key.kb-spin {
    animation: kb-letter-spin 0.7s ease-in-out;
  }
  @keyframes kb-letter-spin {
    0%   { transform: rotate(var(--k-rot, 0deg)) scale(1); }
    50%  { transform: rotate(calc(var(--k-rot, 0deg) + 360deg)) scale(1.35); opacity: 1; }
    100% { transform: rotate(calc(var(--k-rot, 0deg) + 720deg)) scale(1); }
  }

  /* Konami Matrix rain overlay */
  .kb-matrix {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.78);
    z-index: 99998;
    pointer-events: none;
    overflow: hidden;
  }
  .kb-matrix-col {
    position: absolute;
    top: -200px;
    font-family: "JetBrains Mono", monospace;
    font-size: 14px;
    line-height: 1.1;
    color: #3FFF46;
    text-shadow: 0 0 6px #3FFF46, 0 0 12px rgba(63,255,70,0.6);
    white-space: pre;
    animation: kb-matrix-fall 2s linear forwards;
  }
  @keyframes kb-matrix-fall {
    from { transform: translateY(0); opacity: 1; }
    to   { transform: translateY(120vh); opacity: 0; }
  }

  /* Konami progress hint — tiny mono caption bottom-center */
  .kb-konami-hint {
    position: fixed;
    bottom: 16px;
    left: 50%;
    transform: translateX(-50%);
    font-family: "JetBrains Mono", monospace;
    font-size: 0.7rem;
    letter-spacing: 0.18em;
    color: #606060;
    background: rgba(0, 0, 0, 0.7);
    border: 1px solid rgba(63, 255, 70, 0.4);
    padding: 6px 12px;
    z-index: 99997;
    pointer-events: none;
    text-shadow: 0 0 6px rgba(63, 255, 70, 0.6);
  }
  .kb-konami-hint .kb-konami-done {
    color: #3FFF46;
    text-shadow: 0 0 8px rgba(63, 255, 70, 0.8);
  }

  /* CRT power button — premium shadcn-inspired: layered shadows, backdrop
     blur, soft bevel, slow ease. Renders cleanly in Firefox (no SVG, no
     backdrop-filter tricks that Fx handles poorly — just box-shadow stack). */
  .cs-power-btn {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background:
      radial-gradient(circle at 30% 30%, rgba(63, 255, 70, 0.18), rgba(0, 0, 0, 0) 60%),
      #0a0a0a;
    border: 1px solid rgba(63, 255, 70, 0.45);
    color: #3FFF46;
    font-size: 0.95rem;
    font-family: "JetBrains Mono", monospace;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    transition: transform 0.25s cubic-bezier(0.4, 0, 0.2, 1),
                box-shadow 0.25s cubic-bezier(0.4, 0, 0.2, 1),
                border-color 0.25s ease,
                background 0.25s ease;
    text-shadow: 0 0 4px rgba(63, 255, 70, 0.55);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.06),
      inset 0 -1px 0 rgba(0, 0, 0, 0.6),
      0 1px 2px rgba(0, 0, 0, 0.4),
      0 4px 8px rgba(0, 0, 0, 0.35);
  }
  .cs-power-btn:hover {
    transform: translateY(-1px);
    border-color: rgba(63, 255, 70, 0.7);
    background:
      radial-gradient(circle at 30% 30%, rgba(63, 255, 70, 0.28), rgba(0, 0, 0, 0) 60%),
      #0c0c0c;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.08),
      inset 0 -1px 0 rgba(0, 0, 0, 0.7),
      0 2px 4px rgba(0, 0, 0, 0.5),
      0 6px 12px rgba(0, 0, 0, 0.4),
      0 0 16px rgba(63, 255, 70, 0.25);
  }
  .cs-power-btn:active {
    transform: translateY(0);
    box-shadow:
      inset 0 1px 0 rgba(0, 0, 0, 0.6),
      0 1px 2px rgba(0, 0, 0, 0.4);
  }
  .cs-power-btn:focus-visible {
    outline: 2px solid #3FFF46;
    outline-offset: 2px;
  }
  /* Power-off state: both buttons turn RED (standby) and pulse in sync.
     Header button: red bg + border + icon + pulse halo.
     Vintage knob: keeps its chassis, only the indicator + spindle go red
     (handled below by their own rules) + its own synced indicator pulse. */
  body.cs-powered-off .cs-power-btn:not(.cs-power-btn-vintage) {
    background:
      radial-gradient(circle at 30% 30%, rgba(255, 64, 64, 0.22), rgba(0, 0, 0, 0) 60%),
      #0a0a0a;
    border-color: rgba(255, 64, 64, 0.7);
    color: #ff4040;
    text-shadow: 0 0 4px rgba(255, 64, 64, 0.55);
    animation: cs-power-pulse 1.8s ease-in-out infinite;
  }
  @keyframes cs-power-pulse {
    0%, 100% { box-shadow: 0 0 16px rgba(255, 64, 64, 0.5), 0 0 0 1px rgba(0, 0, 0, 0.4); }
    50%      { box-shadow: 0 0 28px rgba(255, 64, 64, 0.9), 0 0 0 1px rgba(0, 0, 0, 0.4); }
  }

  /* CRT power-off — inner screen collapses (tube-TX line→dot), PFP smokes out */
  body.cs-powered-off .terminal-body {
    transform-origin: center center;
    animation: crt-power-off 0.75s cubic-bezier(0.5, 0, 0.5, 1) forwards;
    pointer-events: none;
  }
  /* Power-off sequence — staged like a real CRT dying:
     1. t=0   → screen text fuzzes out (keypad caps, labels, FX icons)
     2. t=0.4 → hardware dims (keyboard, launch pad, floating keys, terminal chassis)
     The terminal-body still does its CRT collapse in parallel. */
  /* Phase 1: text fuzzes to nothing */
  body.cs-powered-off .cs-sd-cap,
  body.cs-powered-off .cs-sd-label,
  body.cs-powered-off .cs-sd-launch,
  body.cs-powered-off .cs-fx-key svg,
  body.cs-powered-off .cs-fx-key-img {
    animation: cs-text-fuzz-out 0.5s ease-out forwards;
  }
  @keyframes cs-text-fuzz-out {
    0%   { filter: blur(0); opacity: 1; }
    60%  { filter: blur(6px); opacity: 0.4; }
    100% { filter: blur(12px); opacity: 0; }
  }
  /* Keyboard SVG labels (letters + wordmark image on space bar) also vanish */
  body.cs-powered-off .kb text,
  body.cs-powered-off .kb image {
    opacity: 0;
    transition: opacity 0.4s ease;
  }
  /* Phase 2: hardware darkens (delay 0.4s so text goes first) */
  body.cs-powered-off .cs-keyboard,
  body.cs-powered-off .cs-streamdeck {
    opacity: 0.18;
    filter: grayscale(1) brightness(0.35);
    pointer-events: none;
    transition: opacity 0.5s ease 0.4s, filter 0.5s ease 0.4s;
  }
  body.cs-powered-off .cs-key {
    opacity: 0;
    filter: grayscale(1) brightness(0.4);
    pointer-events: none;
    transition: opacity 0.5s ease 0.4s, filter 0.5s ease 0.4s;
  }
  /* Terminal chassis (bar + foot + the "live" dot, GH/X icons, dividers)
     fully disappears — not just dim, gone. The screen body does its CRT
     collapse in parallel. */
  body.cs-powered-off .terminal-bar,
  body.cs-powered-off .terminal-foot {
    opacity: 0;
    transition: opacity 0.4s ease;
  }
  /* NOTHING is clickable when power is off — except the power buttons,
     music button, and audio toggle (audio needs to be controllable from
     the dead state so visitors can opt in before booting). */
  body.cs-powered-off a,
  body.cs-powered-off button:not(.cs-power-btn):not(.cs-fx-music):not(.cs-audio-btn) {
    pointer-events: none;
  }
  body.cs-powered-off .terminal-watermark {
    animation: pfp-smoke-out 0.75s ease-out forwards;
    pointer-events: none;
  }
  /* Live pulse indicator goes dark with the screen */
  body.cs-powered-off .terminal-pulse {
    opacity: 0;
    transition: opacity 0.4s ease;
  }
  .terminal-body {
    transform-origin: center center;
    transition: transform 0.55s cubic-bezier(0.4, 0, 0.4, 1);
  }
  .terminal-watermark {
    transition: opacity 0.4s ease, filter 0.4s ease;
  }
  @keyframes pfp-smoke-out {
    0%   { opacity: 0.13; filter: blur(0); transform: translateX(-50%) scale(1); }
    50%  { opacity: 0.08; filter: blur(6px); transform: translateX(-50%) scale(1.08); }
    100% { opacity: 0;    filter: blur(20px); transform: translateX(-50%) scale(1.2); }
  }
  @keyframes crt-power-off {
    0%   { transform: scale(1, 1);     filter: brightness(1); opacity: 1; }
    40%  { transform: scale(1, 0.003); filter: brightness(4) saturate(2); opacity: 1; }
    70%  { transform: scale(1, 0.003); filter: brightness(2); opacity: 1; }
    100% { transform: scale(0.0008, 0.003); filter: brightness(0); opacity: 0; }
  }
  /* CRT power-on — line -> full screen with a brief blur fuzz as it warms up.
     PFP gets its own smoke-in animation so the portrait doesn't jump with the body scale. */
  body.cs-powering-on .terminal-body {
    transform-origin: center center;
    animation: crt-power-on 0.8s cubic-bezier(0.4, 0, 0.4, 1) forwards;
  }
  body.cs-powering-on .terminal-watermark {
    animation: pfp-smoke-in 1.1s ease-out forwards;
  }
  body.cs-powering-on .terminal-pulse {
    animation: pulse-fade-in 0.8s ease-out 0.4s both;
  }
  @keyframes crt-power-on {
    0%   { transform: scale(0.0008, 0.003); filter: brightness(0); opacity: 0; }
    25%  { transform: scale(1, 0.003); filter: brightness(3) blur(1.5px); opacity: 1; }
    55%  { transform: scale(1, 1);     filter: brightness(1.6) blur(0.6px); opacity: 1; }
    80%  { transform: scale(1, 1);     filter: brightness(1.2) blur(0.2px); opacity: 1; }
    100% { transform: scale(1, 1);     filter: brightness(1) blur(0); opacity: 1; }
  }
  /* PFP smokes in — heavy blur fading to crisp, no scale (prevents the jump).
     Preserve translateX(-50%) which is the watermark's normal centering transform. */
  @keyframes pfp-smoke-in {
    0%   { opacity: 0;    filter: blur(24px) brightness(2); transform: translateX(-50%) scale(1); }
    40%  { opacity: 0.05; filter: blur(14px) brightness(1.6); transform: translateX(-50%) scale(1.04); }
    75%  { opacity: 0.10; filter: blur(5px) brightness(1.2); transform: translateX(-50%) scale(1.01); }
    100% { opacity: 0.13; filter: blur(0) brightness(1); transform: translateX(-50%) scale(1); }
  }
  @keyframes pulse-fade-in {
    from { opacity: 0; }
    to   { opacity: 1; }
  }

  /* Reduced motion — only kill AMBIENT auto-playing animations.
     User-triggered effects stay — they're explicit user actions. */
  @media (prefers-reduced-motion: reduce) {
    .cs-pulse { animation: none !important; opacity: 0.7; }
    body.cs-powered-off .cs-power-btn,
    body.cs-powered-off .cs-power-btn-vintage-icon { animation: none !important; }
  }

  /* Inline pill tabs inside the terminal-bar — modern, like real terminal
     apps. No orb, no hanging. Sits left of the socials. */
  .terminal-tabs {
    display: inline-flex;
    align-items: center;
    gap: 2px;
    margin-right: auto;
  }
  .terminal-tab {
    font-family: "JetBrains Mono", monospace;
    font-size: 0.6rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.4);
    background: transparent;
    border: 1px solid transparent;
    padding: 4px 12px;
    cursor: pointer;
    border-radius: 4px;
    transition: color 0.15s ease, background 0.15s ease, border-color 0.15s ease;
  }
  .terminal-tab:hover {
    color: rgba(255, 255, 255, 0.75);
    background: rgba(255, 255, 255, 0.04);
  }
  .terminal-tab.is-active {
    color: #fff;
    background: rgba(63, 255, 70, 0.08);
    border-color: rgba(63, 255, 70, 0.25);
    text-shadow: 0 0 8px rgba(63, 255, 70, 0.4);
  }
  /* GH pinned far left, X pinned far right, live centered between.
     Socials + X use flex:1 with their original justify directions
     (flex-start / flex-end) so the pulse stays in the middle.
     Tabs injected into socials sit right after GH with a 10px gap. */
  .terminal-bar .terminal-socials {
    flex: 1 1 auto !important;
    justify-content: flex-start !important;
    gap: 10px;
  }
  .terminal-bar .terminal-x {
    flex: 1 1 auto !important;
    justify-content: flex-end !important;
  }
  .terminal-bar .terminal-socials a,
  .terminal-bar .terminal-x a {
    width: 30px;
    height: 30px;
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.06);
    color: rgba(255, 255, 255, 0.6);
    justify-content: center !important;
    transition: color 0.15s ease, background 0.15s ease, border-color 0.15s ease, transform 0.15s ease;
  }
  .terminal-bar .terminal-socials a:hover,
  .terminal-bar .terminal-x a:hover {
    background: rgba(63, 255, 70, 0.1);
    border-color: rgba(63, 255, 70, 0.35);
    color: #3FFF46;
    transform: translateY(-1px);
  }
  /* Swap body children in place — body never resizes */
  .terminal-body.music-mode > *:not(.term-music-content) { display: none; }
  .term-music-content { display: none; }
  .terminal-body.music-mode .term-music-content {
    display: flex;
    flex-direction: column;
    gap: 6px;
    height: 100%;
    color: #3FFF46;
    font-family: "JetBrains Mono", monospace;
    font-size: 0.78rem;
    padding: 4px 0;
  }
  .term-music-head { color: #aaa; }
  .term-music-head::before { content: "$ "; color: #3FFF46; }
  .term-music-tracks { display: flex; flex-direction: column; gap: 2px; margin: 4px 0 8px; }
  .term-music-track {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 2px 0;
    color: #888;
    cursor: pointer;
  }
  .term-music-track:hover { color: #fff; }
  .term-music-track.is-playing {
    color: #3FFF46;
    text-shadow: 0 0 6px rgba(63, 255, 70, 0.6);
  }
  .term-music-track-num { color: #3FFF46; min-width: 22px; }
  .term-music-track-name { flex: 1; }
  .term-music-fmt {
    font-size: 0.58rem;
    padding: 0 4px;
    border: 1px solid currentColor;
    letter-spacing: 0.08em;
  }
  .term-music-fmt-cassette { color: #06B6D4; }
  .term-music-fmt-record { color: #A855F7; }
  .term-music-help { color: #555; font-size: 0.62rem; letter-spacing: 0.04em; }
  .term-music-viz {
    display: flex;
    align-items: flex-end;
    gap: 2px;
    height: 22px;
    margin-top: 8px;
  }
  .term-music-viz span {
    flex: 1;
    background: #3FFF46;
    height: 12%;
    min-height: 2px;
    box-shadow: 0 0 2px rgba(63, 255, 70, 0.6);
    transition: height 0.08s linear;
  }
  .term-music-vol-row {
    display: flex;
    align-items: center;
    gap: 6px;
    color: #888;
    font-size: 0.7rem;
    margin-top: 6px;
  }
  .term-music-vol-bars { display: inline-flex; gap: 1px; }
  .term-music-vol-bar {
    display: inline-block;
    width: 3px;
    height: 10px;
    background: #2a2a2a;
  }
  .term-music-vol-bar.is-on {
    background: #3FFF46;
    box-shadow: 0 0 2px rgba(63, 255, 70, 0.6);
  }
  /* Music tab hides when the desk is powered off */
  body.cs-powered-off .terminal-tabs,
  body.cs-powered-off .term-music-content { display: none !important; }

  /* Keyboard hover ALSO enlarges the stream deck by the same amount
     (0.02 scale lift) — they breathe together as one unit. */
  @media (min-width: 769px) {
    .cs-keyboard-stage:hover .cs-streamdeck .cs-sd-bezel {
      transform: scale(1.02);
      transition: transform 0.3s cubic-bezier(0.4, 0, 0.4, 1);
    }
  }
</style>

